matrix:
  include:
    - os: osx
      osx_image: xcode10.2
      language: node_js
      node_js: "10"

# インストールする前に実行する
before_install:
  - brew install yarn
  - brew install wine

install:
  - yarn install

script:
  - yarn run build
